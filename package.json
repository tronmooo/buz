{
  "name": "omnibusiness-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Unified business management platform with AI capabilities",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "db:migrate": "pnpm --filter @omnibusiness/backend db:migrate",
    "db:studio": "pnpm --filter @omnibusiness/backend db:studio"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.6",
    "prettier": "^3.1.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
